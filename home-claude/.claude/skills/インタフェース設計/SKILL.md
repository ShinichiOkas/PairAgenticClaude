---
name: インタフェース設計
description: "インタフェースは「呼ぶ側が知らなくてよいこと」を決める行為である。（技能領域: 実装 ／ 型: 技法 ／ maturity: forming）"
---

<!-- 自動生成。手で編集しない。正は skills/インタフェース設計.md -->

| 欄 | 値 |
|---|---|
| `capability` | 実装 |
| `type` | 技法 |
| `layer` | ドメイン |
| `applies_when` | **未測定** |
| `maturity` | forming |
| `observations` | 1 |
| `provenance` | AgentRoleDesign 2026-09-01 の一括起案。**実走で当てて昇格させる** |

> ✅ **forming —— 実走で当てた（2026-09-01・waterfall-extended / Haiku）。**
> 観測 1 件。**成果物と実行結果で裏を取っている。**

**インタフェースは「呼ぶ側が知らなくてよいこと」を決める行為である。**
何を公開するかではなく、何を隠すかを決める。

## なぜこの体系で要るか

`設計書` の OUT は「実装の内部の申し合わせ（インタフェース・データ構造・分担の境界）」。
**隠せていないものは、後から必ず依存される。**

## How to apply

- **呼ぶ側が知る必要のある事実を列挙する。** それ以外は隠す
- **エラーの契約も書く。** 何がどんな形で失敗するか
- **変えられないもの（契約）と変えてよいもの（実装）を分けて書く**
- ⚠ **内部の都合が引数に漏れていたら、隠せていない**
