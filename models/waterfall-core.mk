# Waterfall ミニマム構成 — 成果物定義（差し戻し込み）
#
# ★ このファイルが正。docs/roles-coding.md はここから生成される。
#
# 書式:  target : deps
#        #   role    : 役割名（命名は導出できない。人が付ける）
#        #   task    : タスク名（command。手順ではなく識別子）
#        #   by      : agent | machine | human
#        #   blocks  : 明示的な遮断（見ると判断が汚染されるもの）
#        #   checks  : 検査対象
#        #   gate    : 人間ゲート識別子
#        #   writes  : 書き込み先
#        #   tools   : read | write | exec（**導出できない**。実行が要る rule だけ宣言する）
#        #   agent.<欄> : ハーネスへ素通しする値（effort / permissionMode / isolation 等）
#        #   out     : OUT の内訳（繰り返し可）
#        #   post    : 事後条件（繰り返し可）
#
# deps の 3 種（P-53）—— **存在しないときの意味が違う**:
#        X            必須 … 存在しなければ**走れない**（待つ）
#        X_v(k-1)     前版 … 初回は必ず存在しない。**空として読み、走る**
#        X?           任意 … 存在しなければ**空として読み、走る**。空だったと申告する
#   ⚠ 作る担い手が居るものを ? にしてはならない（待たずに走る）

# ══════ 起点：外から与えられる。誰も作らない ══════
要望 :
#   agent.path : inbox/要望.md
用語資産 :
#   agent.path : inbox/用語資産.md
既存システムの制約 :
#   agent.path : inbox/既存システムの制約.md
実行環境 :
#   agent.path : inbox/実行環境.md

# ══════ コア成果物 ══════
要求 : 要望, 用語資産?, 要求への差し戻し_v(r-1)
#   agent.path : upstream/要求.md
#   role    : 意図取得者
#   task    : elicit_intent
#   blocks  : 実装物, 実行可能物
#   gate    : H1
#   writes  : 要求
#   out     : 曖昧な語ごとの定義（含むもの／含まないものまで）
#   out     : 確認の問い —— **疑問形であること**。平叙文は「読んでおいてください」で済むが、疑問文は答えないと先へ進めない（P-13）
#   out     : 理由（入力に無かった知識・判断の由来）＋ 残留リスク
#   post    : 要望に現れた曖昧な語が**すべて**定義されている（入力と突き合わせて検査できる・P-16）
#   post    : 定義は「含むもの」と「含まないもの」の両方を書いている
#   post    : 確定は人間の回答による。自分で定義を選んでいない（選べば「要求にないことの追加」にあたる）
#   post    : **問いに依存関係が付いている**（上位に答えると下位が確定するか消える）
# ↑ G-39 の是正。**問いを絞ってはならない** —— 絞ることは「どの曖昧さが重要か」を
#   選ぶことであり、「自分で定義を選んでいない」に違反する。
#   絞るのではなく**順序を付ける**。A の帯域は、問いの数ではなく
#   **「A が答えた数 ÷ 確定した数」**で測る（試走の実測: 6 / 39 ≒ 0.15）

# ══════ 検査接点 —— 遮断された二者が、合意なしに噛み合うための契約 ══════
# 実測（2026-09-06・calc-gui 実走）:
#   **受入検査 149 ケースのうち、実行できたのは 18 件だった。**
#   受入検査設計者が暫定で定めた検査接点（ATP-01）を、構築者が作っていなかった。
#   両者は互いに遮断されているので、**接点を合意する経路が無い。**
#   ⚠ 遮断が正しく働いた結果として、検査がほぼ実行不能になった。
#
# なぜ成果物として割るか（P-58）:
#   **遮断で届かない領域は、受け手を強くするのではなく、成果物を立てて埋める。**
#   `設計保証` が「外形から検められない項目」を埋めたのと同じ形である（P-45 の 3 例目）。
#
# ⚠ なぜ `仕様` より**前**に置くか:
#   `仕様` にはゲート H2 が付いている。後ろに置くと SoD 第2形の分離線が立ち、
#   **仕様作成者が兼ねられなくなる**（師匠の裁定「仕様が決める」に反する）。
#   前に置けば線は立たない。副次的に「**観測できる形を先に決めてから中身を書く**」になる。
検査接点 : 要求, 既存システムの制約?, 検査接点への差し戻し_v(k-1)
#   role    : 仕様作成者
#   task    : define_test_interface
#   blocks  : 実装物, 実行可能物, 受入検査仕様
# ↑ 出てきたコードにも検査にも合わせない。合わせた時点で契約ではなくなる
#   writes  : 検査接点
#   agent.path : upstream/検査接点.md
#   out     : **外から何を、どう叩けば、何が観測できるか**（呼び名・引数・戻り・出し先）
#   out     : 要求の各項目について、それを**外形から判定するのに要る観測点**
#   out     : **判定に要るのに提供できない観測点**の一覧（`設計保証` が引き受ける材料になる）
#   out     : 理由 ＋ 残留リスク
#   post    : **構築者が deps だけを見て実装できる**（暫定の名前や未定義の引数を含まない）
#   post    : **受入検査仕様の担い手が、実装物を見ずにこれだけで判定手続きを書ける**
#   post    : **内部構造を露出していない** —— 露出させると受入検査がブラックボックスでなくなる
#   post    : 要求から導出できない観測点を足していない（P-23）
#   post    : **提供できない観測点が明示されている**（「全部観測できる」で終わらせない）

仕様 : 要求, 検査接点, 既存システムの制約?, 仕様への差し戻し_v(m-1)
#   agent.path : upstream/仕様.md
#   role    : 仕様作成者
#   task    : write_specification
#   blocks  : 実装物, 実行可能物, 受入検査仕様
#   gate    : H2
#   writes  : 仕様
#   out     : 何を作るか。要求を 100% 説明する
#   out     : 満たさない項目は**理由と扱い**（スコープ外／延期／代案／不可能／**暫定**）
#   out     : 実装言語・対象プラットフォームの制約（P-30。役割ではなく仕様に置く）
#   out     : 理由 ＋ 残留リスク
#   post    : 不足なし —— 要求の項目が黙って落ちていない
#   post    : 過剰なし —— 要求と用語定義から**導出できない**ものを足していない（P-24）
#   post    : 各項目が判定手続きに落とせる（検査するのは 受入検査仕様 の担い手）
#   post    : 「不可能」と判定した項目は範囲外として上げている（deps を満たす OUT が存在しないため）
#   post    : **暫定**として置いた項目には、**解除条件**（誰が何を答えたら差し替えるか）が付いている
# ↑ G-37 の是正。事後条件「過剰なし」と「判定手続きに落とせる」は両立しないことがある
#   —— 上流が「環境の上限」だけ与えて「製品の目標」を与えなかったとき。
#   止まると下流がブロックされるので、暫定値を置いて上げる。
#   **暫定を許すかは方針が決める**（W-6: 扱いの型はプロセス依存）

受入検査仕様 : 仕様, 検査接点, 受入検査仕様への差し戻し_v(j-1)
#   agent.path : acceptance/受入検査仕様.md
#   role    : 受入検査設計者
#   task    : design_acceptance_tests
#   blocks  : 実装物, 実行可能物
# ↑ ソースも動くものも見ない。見れば「あるものに合わせた」検査になる
#   checks  : 仕様
#   writes  : 受入検査仕様
#   out     : 仕様の各項目に対応する判定手続き
#   out     : 判定手続きに落とせなかった仕様項目の一覧（空でなければ 差し戻し指示 の材料になる）
#   out     : **各判定手続きが、対応する仕様項目のどこを判定できないか**（部分的な取りこぼし）
# ↑ G-38 の是正。「落とせた／落とせない」の二値だった。**部分的にしか落とせない場合が無かった**
#   out     : 理由 ＋ 残留リスク
#   post    : 仕様の各項目に判定手続きが対応している（網羅）
#   post    : **「対応している」の定義を満たしている** —— 手続きが no を出す ⟺ 仕様項目が満たされていない。後ろ向き（満たされていないのに no を出さない）が成り立たない部分は明示されている
#   post    : 上流の成果物が自分の事後条件を破っていたら、勝手に読み替えず「暫定」として扱い、解除条件（A が仕様を直すか、この読み替えを承認するか）を付けている
# ↑ G-38 の是正。試走で「外部パッケージの import が無いことを確認」（ソース検査）を
#   「隔離環境で実行して落ちない」に置き換えた。**等価ではない**
#   —— 遅延 import・条件付き import は検出できない。**充足を実施可能性にすり替えていた**
#   post    : 判定が二値である
#   post    : 内部構造に依存しない（ブラックボックス）

実装物_v(n) : 仕様, 検査接点, 実装物_v(n-1), 実装物への差し戻し_v(n-1)
#   agent.path : 実装物
#   role    : 構築者
#   task    : build
#   blocks  : 受入検査仕様
#   writes  : 実装物
#   tools   : exec
# ↑ out が「実行可能なコード」、skill が `ホワイトボックステストの書き方`。
#   **テストは走らせないと効いているか分からない**（実測: 試走の作業空間に pytest のキャッシュが残る）
#   out     : 実行可能なコード
#   out     : ホワイトボックステスト（**必須**。非回帰の合否の deps がこれを要求する）
#   out     : 縮退・フォールバック経路のログ（**必須**。無いと下流のどこからも観測できない）
#   out     : 理由 ＋ 残留リスク
#   post    : 仕様を満たす（不足なし・過剰なし）
#   post    : ホワイトボックステストを含む
#   post    : 縮退経路にログがある

# ══════ ブラックボックス境界 ══════
# 実装物は一枚岩ではない（ソース＋WBテスト＋ログ＋理由）。
# 受入検査が必要なのは「動かせる形」だけなので、そこを切り出す。
# これで「ソースを見ない」＝ブラックボックス性が**遮断として**書けるようになる。
# ══════ 設計保証 —— 外形からは検められない項目を、作った側が保証する ══════
# 現実の「設計保証」（師匠、2026-09-01）:
#   **QA には分からないことを実装者が保証する。実装者はエビデンスの提出義務を負う。**
#
# なぜ成果物として割るか（P-45）:
#   `実行可能物` が「外形」として 実装物 から割れたのと**同じ理由**である。
#   受入検査は 実装物 を遮断されているので、内部に関する仕様項目を検められない（G-58）。
#   **遮断を緩めるのではなく、送り手が選んで渡す成果物を立てる。**
#
# ⚠ これは遮断を通す**制御された経路**である。だから義務と事後条件が付く。
設計保証 : 実装物_v(n), 仕様
#   agent.path : upstream/設計保証
# ↑ ⚠ `.md` を付けない。**成果物が 1 ファイルとは限らない**（実測 2026-09-07）。
#   保証者は `probe.py`（Win32 で実際に窓を列挙する検証プログラム）を添えたため
#   `upstream/設計保証/` というディレクトリになった。
#   `.md` で宣言していると、**存在の観測が false になり毎回起動する。**
#   role    : 構築者
#   task    : assure_design
#   blocks  : 受入検査仕様
# ↑ 検査項目を知ると、それに合わせて保証範囲を選んでしまう
#   writes  : 設計保証
#   out     : **外形からは検められない仕様項目の一覧**（なぜ検められないかつき）
#   out     : 各項目について「どう満たしたか」の主張 ＋ **エビデンス**
#   out     : **保証できなかった項目** —— 残留リスクへ移す
#   post    : 外形から検められる項目を含んでいない（受入検査の仕事を重複させない）
#   post    : **主張だけの項目が無い。**すべてにエビデンスが付いている
#   post    : **エビデンスが、実装物を読まずに検められる形になっている**
#   post    : 仕様に無い項目を保証していない（P-23。**過剰はここに現れる** —— G-70）

実行可能物 : 実装物_v(n)
#   agent.path : 実行可能物
#   by      : machine
#   task    : package
#   writes  : 実行可能物
#   out     : 動かせる形にした成果。**ホワイトボックステスト・構築者の理由・作業ログを落とす**
#   out     : **落としたものの一覧**（何を落としたか、なぜ落としたか）
#   post    : 実装物から決定的に得られている（内容を足していない）
#   post    : **落とすべきものが落ちている** —— `受入検査報告書` の遮断対象が含まれていない
#   post    : **落としたものが列挙されている**（黙って落とさない。黙って残さない）
# ↑ G-73 の是正（実測 2026-09-06・calc-gui 実走）。
#   旧: out が「外から観測できる面だけ」という**形容**で、何を落とすかを名指ししていなかった。
#   post も「足すな」しか言わず「落とせ」を言っていなかった。
#   **結果、実行可能物は実装物の完全な複製になった**（差分は梱包目録 1 件のみ）。
#   ソースも WB テストも構築者の理由も、遮断されたはずの受入検査実施者の手元に届いた。
#
#   ⚠ 対照が同じ日の実走にある —— `inquiry-small.mk` の `提出版` は out に
#   「作業メモ・調査ログ・調査者の理由を落とす」と**名指しした**ので、実際に 27 KB 落ちた。
#   **⇒ 遮断は、それを実現する側の成果物に「何を落とすか」が書かれていないと成立しない**（P-42）。
#
#   ⚠ **ソースは落とせないことがある。** インタプリタ言語では実行可能物がソースそのものである。
#   その場合 `blocks : 実装物` は「実装物というパスを読むな」までしか効かない。
#   **落とせなかったものも「落としたものの一覧」に理由つきで書く**（黙って残さない）。

受入検査報告書 : 実行可能物, 受入検査仕様, 検査接点, 設計保証, 実行環境
#   agent.path : acceptance/受入検査報告書.md
#   role    : 受入検査実施者
#   task    : execute_acceptance_tests
#   blocks  : 実装物
# ↑ **ブラックボックス性は遮断で書ける。** 見ないのはソース・WBテスト・構築者の理由。
#   動かせる形（実行可能物）は deps に持つ —— 持たないと検査できない
#   checks  : 実装物
#   gate    : H3
#   writes  : 受入検査報告書
#   tools   : exec
# ↑ post が「`実行可能物` を作業空間へ複製して実行する」と言っている。
#   **実行は書き込みである**（G-40）
#   out     : 合否（二値）
#   out     : 差分 —— どの項目がどう外れたか
#   out     : 実施条件 —— いつ・どの環境で・どのデータで、**そして実行が環境に何を残したか**（**入力に無い**。これがコアである理由）
#   out     : カバーできなかった領域（**入力に無い**。実行して初めて分かる）
#   out     : 残留リスク（何が起こりうるか ＋ 理由として何を確かめていないか）
#   post    : 受入検査仕様の各項目に結果が対応している
#   post    : 合否が二値である
#   post    : カバーできなかった領域が明示されている（「全部通った」で終わらせない）
#   post    : 欠陥を見つけても直していない —— 合否を no にして差分に書く。修正は 実装物 の再作成に回す
#   post    : **設計保証の各項目に、受理／不受理が付いている**
#   post    : **設計保証を読んだことで、受入検査仕様の項目を増減させていない**
#   post    : **受理したが自分では検証していない項目**を、残留リスクに移している
#   post    : **実行が他人の成果物に書き込んでいない** —— `実行可能物` を作業空間へ複製して実行する
# ↑ G-40 の是正。**実行は書き込みである**（pytest がキャッシュを作る、一時ファイルが出る等）。
#   規定しているのは**成果物への書き込み**だけで、作業空間は自由に使ってよい。
#   **復元してはならない** —— 復元は「なかったことにする」ので、何が起きたか分からなくなる
#   post    : **作業領域に閉じない副作用**（実行環境の状態変化）があれば、**実施条件に申告している**


# ══════ 人間：役割ではない（P-12。A は委譲不能） ══════
# ══════ 差し戻し —— **宛先ごとに成果物を割る** ══════
#
# G-74 の是正（実測 2026-09-07・calc-gui-2 の受入検査）。
#
# 旧: `差し戻し指示` は 1 つの成果物で、宛先は本文に「差し戻し先（**暫定**）」と
#     書くだけだった（G-35: 一意に決まらない）。
#     **その結果、機械はどこへ差し戻されたかを読めない。**
#     置いた瞬間に `仕様` から下流 9 体が全部起動する —— 実装物 1 件の欠陥を直すために。
#
# 新: **宛先ごとに別の成果物にする。宛先は「どのファイルを書いたか」で表す。**
#     本文に宛先を書かない。**依存グラフが routing を運ぶ。**
#
# ⚠ **これは G-35 を否定しない。** G-35 は「差し戻し先は一意に決まらない」と言った。
#    一意に決まらないなら、**A は複数のファイルを書けばよい。**
#    「1 つ選べ」を強いるのではなく、「選んだものを全部、別々に置け」に変えている。
#
# ⚠ 5 つ置いた理由 —— **A はどのゲートでも差し戻せる**（G-53）と宣言したのに、
#    受け手の側は 3 つ（仕様・受入検査仕様・実装物）しか dep を持っていなかった。
#    `要求` と `検査接点` に差し戻す経路が**存在しなかった**。ここで閉じる。
#
# 共通（5 つとも同じ）:
#   by / task / checks / gate は同一。deps は**全部任意**（実行可能条件が無条件になり、
#   A はどのゲートでも書ける）。**out から「差し戻し先」が消えている**のが要点である。

要求への差し戻し : 受入検査報告書?, 変異試験の合否?, 非回帰の合否?, 仕様?, 要求?
#   agent.path : 差し戻し/要求.md
#   by      : human
#   task    : adjudicate_rework
#   checks  : 受入検査報告書
#   gate    : H3
#   writes  : 要求への差し戻し
#   out     : なぜ差し戻されるか —— **意図取得者の deps の語彙**で、着手できる粒度で
#   out     : 同一項目の再発回数（探針を起動すべき信号。実現不能の証明ではない）
#   post    : 受け手は**これだけを見て**修正に着手できる
#   post    : 受け手が読めない語彙を含まない
#   post    : **どのゲートで差し戻したかが書かれている**（判定の根拠＝検査ケースの内容を含まない）
#   post    : **宛先を本文に書いていない。** 宛先はこの文書が存在すること自体である
# ↑ 本文に宛先を書くと、機械が読む宛先（ファイル）と食い違いうる。**二重に持たない**

検査接点への差し戻し : 受入検査報告書?, 変異試験の合否?, 非回帰の合否?, 仕様?, 要求?
#   agent.path : 差し戻し/検査接点.md
#   by      : human
#   task    : adjudicate_rework
#   checks  : 受入検査報告書
#   gate    : H3
#   writes  : 検査接点への差し戻し
#   out     : なぜ差し戻されるか —— **仕様作成者の deps の語彙**で、着手できる粒度で
#   out     : 同一項目の再発回数
#   post    : 受け手は**これだけを見て**修正に着手できる
#   post    : 受け手が読めない語彙を含まない
#   post    : **どのゲートで差し戻したかが書かれている**
#   post    : **宛先を本文に書いていない**

仕様への差し戻し : 受入検査報告書?, 変異試験の合否?, 非回帰の合否?, 仕様?, 要求?
#   agent.path : 差し戻し/仕様.md
#   by      : human
#   task    : adjudicate_rework
#   checks  : 受入検査報告書
#   gate    : H3
#   writes  : 仕様への差し戻し
#   out     : なぜ差し戻されるか —— **仕様作成者の deps の語彙**で、着手できる粒度で
#   out     : 同一項目の再発回数
#   post    : 受け手は**これだけを見て**修正に着手できる
#   post    : 受け手が読めない語彙を含まない
#   post    : **どのゲートで差し戻したかが書かれている**
#   post    : **宛先を本文に書いていない**

受入検査仕様への差し戻し : 受入検査報告書?, 変異試験の合否?, 非回帰の合否?, 仕様?, 要求?
#   agent.path : 差し戻し/受入検査仕様.md
#   by      : human
#   task    : adjudicate_rework
#   checks  : 受入検査報告書
#   gate    : H3
#   writes  : 受入検査仕様への差し戻し
#   out     : なぜ差し戻されるか —— **受入検査設計者の deps の語彙**で、着手できる粒度で
#   out     : 同一項目の再発回数
#   post    : 受け手は**これだけを見て**修正に着手できる
#   post    : 受け手が読めない語彙を含まない
#   post    : **どのゲートで差し戻したかが書かれている**
#   post    : **宛先を本文に書いていない**
# ↑ ⚠ **判定の根拠（検査ケースの内容）を書いてはならない。**
#   受入検査設計者は 実装物 / 実行可能物 を遮断されている。
#   「どのケースがどう落ちたか」を渡すと、**出てきたものに合わせた検査**に変わる

実装物への差し戻し : 受入検査報告書?, 変異試験の合否?, 非回帰の合否?, 仕様?, 要求?
#   agent.path : 差し戻し/実装物.md
#   by      : human
#   task    : adjudicate_rework
#   checks  : 受入検査報告書
#   gate    : H3
#   writes  : 実装物への差し戻し
#   out     : なぜ差し戻されるか —— **構築者の deps の語彙**（仕様・検査接点）で、着手できる粒度で
#   out     : 同一項目の再発回数
#   post    : 受け手は**これだけを見て**修正に着手できる
#   post    : 受け手が読めない語彙を含まない
#   post    : **どのゲートで差し戻したかが書かれている**
#   post    : **宛先を本文に書いていない**
# ↑ ⚠ **受入検査仕様の内容を書いてはならない。** 構築者は受入検査仕様を遮断されている。
#   「仕様のどの項目が満たされていないか」までは渡せる（仕様は構築者の dep である）

# ══════ 機械：役割ではない（P-11） ══════
# ══════ テストが「効いているか」を機械が検める ══════
# 構築者以外にホワイトボックステストは書けない（検証者は実装物を遮断されている）。
# しかし作った者が自分のテストを書くのは SoD 第1形の違反であり、
# 実測ではテストが実装と同じ盲点を共有する（同じ流儀で全部が同時に盲目になる）。
# **分離できないが兼務も危険。だから機械の検査を置く。**
変異試験の合否 : 実装物_v(n)
#   agent.path : checks/変異試験の合否.md
#   by      : machine
#   task    : run_mutation_testing
#   writes  : 変異試験の合否
#   out     : 注入した変異のうち、**検出されなかったもの**の一覧
#   post    : 決定論的である
#   post    : 検出されなかった変異が空でなければ不合格。「通った」で終わらせない

非回帰の合否 : 実装物_v(n), 実装物_v(n-1)
#   agent.path : checks/非回帰の合否.md
#   by      : machine
#   task    : run_regression
#   writes  : 非回帰の合否
#   post    : 決定論的である。「既知の赤」で通さない

残留リスク報告 : 要求, 仕様, 受入検査仕様, 実装物_v(n), 受入検査報告書
#   agent.path : checks/残留リスク報告.md
#   by      : machine
#   task    : aggregate_residual_risk
#   writes  : 残留リスク報告
#   post    : 各コア成果物の残留リスクが漏れなく集約されている
